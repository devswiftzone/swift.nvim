-- This file is automatically loaded by Neovim
-- Use it for plugin-wide configurations that should run before user config

if vim.g.loaded_swift_nvim then
  return
end
vim.g.loaded_swift_nvim = true

-- You can add global commands or settings here that need to be available
-- before the user calls setup()
